import { StyleSheet } from 'react-native';
import { spacing } from '~/modules/theme';

export const styles = StyleSheet.create({
  buttonContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
  sensorTitle: { marginBottom: spacing.small },
});
