import {
  colors,
  gradients,
} from './colors';
import {
  dimensions,
  spacing,
} from './dimensions';
import {
  textStyles,
  fontWeight,
} from './typography';

export const MODULE_NAME = 'THEME_MODULE';

export {
  colors,
  gradients,
  dimensions,
  spacing,
  textStyles,
  fontWeight,
};
